public class ListOfInvoices_Controller {
    public list<Invoice__c> Invlst {get;set;}
    public Id recid {get;set;}
    
    public  ListOfInvoices_Controller(){
        Invlst = [Select id,Name,Account__r.Name, Days_Overdue__c, Due_Date__c, Invoice_Date__c, Amount_Paid__c, Amount_Due__c, Status__c,Total__c  from Invoice__c ];
        system.debug('INVLst=='+Invlst);
    }
    
    /*public PageReference doRedirect(){
        PageReference pageRef = new PageReference('/apex/doRedirect?id='+recid);
        pageRef.setRedirect(true);
        return pageRef;
    }*/
}	
